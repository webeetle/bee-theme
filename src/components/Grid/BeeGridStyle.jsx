import {
  danger, dark,
  drawerWidth,
  drawerWidthCollapsed, info, success, warning
} from "../main-theme";
import {hexToRgba} from './../styles/utils';


const BeeGridStyle = theme => ({

});

export default BeeGridStyle;
